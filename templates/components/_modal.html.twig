<div id="{{ id|default('modal-container') }}" class="modal-overlay hidden">
	<div class="modal-card">
		<div class="modal-header">
			<h3 id="modal-title">{{ title|default('Formulaire') }}</h3>
			<button onclick="UI.closeModal()" class="btn-close">×</button>
		</div>
		<div id="modal-body" class="modal-content">
			{{ body|default('')|raw }}
		</div>
		<div class="modal-footer">
			<button id="modal-save" class="btn-glow">
				<i class="fas fa-save"></i>
				{{ save_label|default('Enregistrer') }}
			</button>
		</div>
	</div>
</div>
